<!DOCTYPE html>
<html>
        <h1>Today</h1>
        <% day = %w(Sunday Monday Tuesday Wednesday Thursday Friday Saturday) %>
        <% month = %w(January February March April May June July August September October November December) %>
        
        <p>The date is <%= day[currentTime.wday] + ', ' + month[currentTime.month - 1] + ' ' + format('%02d', currentTime.mday) + ', ' + currentTime.year.to_s %>.</p>
        
</html>